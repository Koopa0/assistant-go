// Package memory implements the assistant's memory system, allowing it to store,
// retrieve, and manage information over time. This includes different types
// of memory such as short-term working memory and persisted long-term memory.
//
// It provides services for CRUD operations on memory entries, search capabilities,
// and defines the structure of memory items, including their content, type,
// importance, and metadata. The package aims to provide a flexible memory
// foundation for contextual understanding and personalization.
package memory
